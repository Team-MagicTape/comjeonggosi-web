import CustomLink from "@/shared/ui/CustomLink";
import { ToolCase } from "lucide-react";
import TodayQuestionForm from "@/entities/today-question/ui/TodayQuestionForm";

const TodayQuestion = () => {
  return (
    <div>
      <TodayQuestionForm/>
    </div>
  );
};

export default TodayQuestion;
