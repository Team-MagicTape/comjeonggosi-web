export interface User {
  id: string,
  provider: string,
  providerId: string,
  email: string,
  nickname: string,
  profileImageUrl: string,
  lastLoginAt: string,
  createdAt: string,
  updatedAt: string
}