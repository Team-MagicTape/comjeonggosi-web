export interface User {
  id: number,
  provider: string,
  providerId: string,
  email: string,
  nickname: string,
  profileImageUrl: string,
  lastLoginAt: string,
  createdAt: string,
  updatedAt: string
}